#include <stdio.h>
#include <cs50.h>
int main(int argc, string argv[])
{
    int cantidad_argumentos = argc;
    if (cantidad_argumentos < 1)
    {
        if(contrasena == 0)
        {

        }

        string valor = argv[1];
        printf("%s\n", argc);
    }
    else
    {
        printf("Contraseña incorreta, vuelva a intentar○\n");
        return 1;
    }

    return 0;
}

