
#include <stdio.h>
#include <math.h>
#include <cs50.h>

int main()
{
    int f = 50;
    int r = 35;
    int c = 40;

    printf("el numero de la suma de las variables f + r + c es : ", f + r + c);

    printf("el numero de la multiplicacion de las variables r + f + c es : ", r * f * c);

    printf("el numero de la resta de las variables c + r + f es : ", c - r - f);

}
